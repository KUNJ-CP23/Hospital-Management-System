@model HMS.Models.AppointmentModel

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Appointment Add/Edit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="background-color: #f8f9fa;">
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-header text-white" style="background-color: rgba(37, 49, 125)">
                        <h4 class="mb-0">Appointment Add / Edit</h4>
                    </div>
                    <div class="card-body">
                        <form asp-controller="Appointment" asp-action="AppointmentAddEdit" method="post">
                            <input type="hidden" asp-for="AppointmentId" />

                            <div class="mb-3">
                                <label asp-for="AppointmentDate" class="form-label">Appointment Date & Time</label>
                                <input asp-for="AppointmentDate" type="date" class="form-control" required />
                            </div>

                            <div class="mb-3">
                                <label asp-for="AppointmentStatus" class="form-label">Status</label>
                                <input asp-for="AppointmentStatus" type="text" class="form-control" placeholder="Enter status (e.g., Confirmed, Cancelled)" required />
                            </div>

                            <div class="mb-3">
                                <label asp-for="Description" class="form-label">Description</label>
                                <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                            </div>

                            <div class="mb-3">
                                <label asp-for="SpecialRemarks" class="form-label">Special Remarks</label>
                                <input asp-for="SpecialRemarks" type="text" class="form-control" />
                            </div>

                            <div class="mb-3">
                                <label asp-for="TotalConsultedAmount" class="form-label">Total Consulted Amount</label>
                                <input asp-for="TotalConsultedAmount" type="number" class="form-control" required />
                            </div>

                            <div class="mb-3">
                                <label asp-for="DoctorID" class="form-label">Doctor</label>
                                <select asp-for="DoctorID" class="form-select" asp-items="@(new SelectList(ViewBag.DoctorList, "DoctorID", "Name"))" required>
                                    <option value="">Select Doctor</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label asp-for="PatientID" class="form-label">Patient</label>
                                <select asp-for="PatientID" class="form-select" asp-items="@(new SelectList(ViewBag.PatientList, "PatientID", "Name"))" required>
                                    <option value="">Select Patient</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label asp-for="UserID" class="form-label">User</label>
                                <select asp-for="UserID" class="form-select" asp-items="@(new SelectList(ViewBag.UserList, "UserID", "UserName"))" required>
                                    <option value="">Select User</option>
                                </select>
                            </div>

                            <div class="text-end">
                                <button type="submit" class="btn btn-outline-success">Save</button>
                                <a asp-controller="Appointment" asp-action="AppointmentList" class="btn btn-secondary">Cancel</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
